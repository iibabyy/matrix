[package]
name = "matrix"
version = "0.1.0"
edition = "2024"

[features]
default = ["clap"]
clap = ["dep:clap"]

[[bin]]
name = "projection"
path = "src/projection.rs"

[dependencies]
clap = { version = "4.6.1", optional = true, features = ["derive"] }
